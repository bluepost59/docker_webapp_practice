version: '3'
services:
    app:
        build:
            context: app
            dockerfile: dockerfile
        volumes:
            - ./app:/app
        ports:
            - 5000:5000
        command: python3 /app/app.py
            # reverse_proxy:
            #     image: nginx

    db:
        image: mongo
        volumes:
            - ./db:/data/db
